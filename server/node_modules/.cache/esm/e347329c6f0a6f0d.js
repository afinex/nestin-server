let express,readdirSync;_6f0‍.w("express",[["default",["express"],function(v){express=v}]]);_6f0‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);

require('dotenv').config();

const app = express();
readdirSync('./routes').map((e_route)=>{
    app.use('/api', require(`./routes/${e_route}`));
});

const port = process.env.PORT || 8001;
app.listen(port, () => console.log('Server is running on port 8001'))
